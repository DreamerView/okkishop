/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com'], // Добавьте сюда домен, с которого вы загружаете изображения
    },
};

export default nextConfig;